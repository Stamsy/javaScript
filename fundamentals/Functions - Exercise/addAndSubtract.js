function addAndSubtract(one, two, tree) {
    let firstSum = sum(one, two);
    let finalResult = substract(firstSum, tree)

    console.log(finalResult);
    function sum(first, second) {
        return one + two
    }
    function substract(first, second) {
        return firstSum - tree
    }
}
addAndSubtract(23, 6, 10)
function tseamAccount(games) {
    let gamseL = games.length - 1;
    let index = 0;
    let gameCount = games[index];
    gameCount.split(' ');
    let currentGame = [];

    for(let i = 0; i<gameCount.length;i++){
        
        currentGame.push(gameCount)
    }

 
    
}
tseamAccount(['CS WoW Diablo',
'Install LoL',
'Uninstall WoW',
'Update Diablo',
'Expansion CS-Go',
'Play!'])